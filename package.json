{
  "private": true,
  "name": "external-auth-rule-eval-worker",
  "version": "1.0.0",
  "description": "An example worker that can be used to quickly implement the service side for the Access external evaluation rule",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "James Royal <jroyal@cloudflare.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
